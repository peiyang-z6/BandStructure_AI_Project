# BandStructure AI Project — pinned environment (Phase 6 hardening)
#
# Every version below is the EXACT version validated by the accepted
# v6 formal run (aflow_noleak_v6_30k_seed42_metricfix, GPU-only V100).
# Runtime: WSL2 conda env `bandstructure-ai` (Python 3.11.15).
#
#   conda env create -f environment.yml   # full snapshot, preferred path
#   pip install -r requirements.txt       # equivalent pip surface
#
# NOTE: the GUI/CV toolchain (tkinter, opencv, matplotlib, TF inference)
# runs inside the same WSL environment; the Windows-native Python is not used.

# ── Core ML ──────────────────────────────────────────────────────────────
tensorflow==2.21.0
keras==3.15.1
tensorboard==2.21.0
numpy==1.26.4
h5py==3.14.0
scipy==1.17.1
scikit-learn==1.9.0
pandas==3.0.5
protobuf==7.35.1

# ── Materials APIs ───────────────────────────────────────────────────────
mp-api==0.46.4
emmet-core==0.87.1
pymatgen==2026.5.4
pymatgen-core==2026.7.31
spglib==2.7.0
monty==2026.7.16

# ── GUI (tkinter is supplied by the Python distribution) ─────────────────
pillow==12.3.0
matplotlib==3.11.1

# ── Plot-to-Physics / literature mining CV ───────────────────────────────
opencv-python-headless==4.11.0.86
pymupdf==1.28.2

# ── Tuning & reporting ───────────────────────────────────────────────────
optuna==4.9.0
plotly==6.9.0
requests==2.34.2
python-dotenv==1.2.2
pyyaml==6.0.3

# ── Development / verification ───────────────────────────────────────────
pytest==8.4.2
pytest-cov==7.1.0
joblib==1.5.3